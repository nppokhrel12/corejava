package OOP;

public class ConstructorDoc {
	/*
	 * constructor 
	 * used to create object in java 
	 * special type of method with same name as class name
	 * constructor donot have return type 
	 * 
	 * types of const
	 * 
	 * default > used to create object with its default values
	 * syntax
	 * class_name(){
	 * set default values to object
	 * }
	 * 
	 * parameterized constructor > to create object with its dynamic values
	 * syntax 
	 * class_name(arg1,arg2){
	 *set dynamic values to object
	 * }
	 * 
	 * 
	 */
	
}